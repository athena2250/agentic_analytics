# Run from this directory: `pytest tests`
#
# tests/ is the executable form of plan §15 (the MVP definition). It stubs the
# local model, so it needs no running ollama and no network.
[pytest]
testpaths = tests
addopts = -q
